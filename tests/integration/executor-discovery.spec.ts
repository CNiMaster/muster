import{describe,expect,it,vi}from'vitest';import{makeTestDb}from'./setup';import{bindDetectedSystemExecutor,detectSystemExecutor}from'../../src/server/domain/executor-discovery';
describe('official system executor onboarding',()=>{it('detects and binds an existing CLI without copying it',async()=>{const{db,close}=makeTestDb();try{const runtime={which:vi.fn(async()=>'/opt/homebrew/bin/codex'),run:vi.fn(async()=>({stdout:'codex-cli 0.144.1',stderr:'',exitCode:0}))};expect((await detectSystemExecutor('codex-cli',runtime)).managed).toBe(false);const profile=await bindDetectedSystemExecutor(db,'codex-cli',runtime);expect(profile.config.binaryPath).toBe('/opt/homebrew/bin/codex');expect(profile.install.source).toBe('system');const again=await bindDetectedSystemExecutor(db,'codex-cli',runtime);expect(again.id).toBe(profile.id);}finally{close();}});it('does not create a binding when the official CLI is missing',async()=>{const{db,close}=makeTestDb();try{await expect(bindDetectedSystemExecutor(db,'gemini-cli',{which:vi.fn(async()=>null),run:vi.fn()} as any)).rejects.toThrow(/尚未安装/);}finally{close();}});});
