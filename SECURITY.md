# 安全策略 (Security Policy)

## 支持的版本

| 版本 | 支持状态 |
| :--- | :--- |
| main 分支 | ✅ 积极开发（本地构建） |
| 其他 | ❌ 不提供安全修复 |

由于 Muster 是本地优先的桌面级应用，尚未发布正式版本号。发布后本表将按版本更新。

## 报告漏洞

如果你发现安全漏洞，**请勿公开披露**（不要在 Issue 中贴出漏洞细节）。

请通过以下渠道报告：

1. **GitHub Private Advisory**：访问仓库 → `Security` → `Report a vulnerability`（推荐）
2. **邮件**：在仓库 `Security` 页查看维护者联系邮箱

请在报告中包含：

- 漏洞类型与影响范围
- 复现步骤（最小示例优先）
- 受影响的版本/分支
- 你建议的修复方向（可选）

我们承诺：

- **48 小时内**确认收到报告
- **7 天内**给出初步评估与修复计划
- 修复后**公开致谢**（除非你要求匿名）

## 安全设计原则

Muster 的安全模型建立在「本地优先 + 最小权限」之上：

- **100% 本地数据**：数据库、项目文件、记忆全部存储在本机（默认 `~/.muster/`），无云端上报。
- **密钥不落库**：API Key 只从系统环境变量读取，数据库与日志中只保存引用名，不保存明文。
- **执行器隔离**：每个 Task 在独立 git worktree 沙盒中运行，项目间目录隔离。
- **命令白名单**：CLI 一键安装只执行 `manifests.ts` 中代码内置的官方安装命令，不接受任意命令。
- **路径校验**：备份导入的 skill 文件路径由服务端重新扫描定位，客户端不能指定任意路径读取文件。
- **备份不含密钥**：导出备份只含结构化配置与凭据引用，不含明文密钥与项目文件。

## 已知限制（本地应用）

- Muster 会代表你在本机执行 AI CLI 命令（如 `claude`）。请确认你信任安装的 CLI 及其官方安装脚本来源。
- 工作区迁移会物理移动整个项目目录；请确保迁移前工作台已全部下班、无运行中任务。
- 若你自行修改代码或使用第三方插件，请自行评估其安全性。

## 安全相关功能开关

| 环境变量 | 作用 |
| :--- | :--- |
| `MUSTER_HOME` | 程序数据根目录（默认 `~/.muster`） |
| `MUSTER_AUTO_EXECUTOR_DISCOVERY=false` | 关闭启动时自动检测系统 CLI |
| `MUSTER_SKIP_PERMISSIONS=true` | 跳过权限审批（仅本地调试用，不建议生产） |
